# Backend Environment Variables
# Copy isi file ini ke file .env di folder backend/

# Database Configuration (PostgreSQL)
# Opsi 1: Gunakan variabel terpisah
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=aquabiodiversa
DB_PORT=5432
DB_SSL=false

# Opsi 2: Gunakan connection string (untuk Supabase)
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
# Jika menggunakan Supabase, uncomment baris di atas dan isi dengan connection string dari Supabase

# JWT Secret (Ganti dengan secret key yang kuat untuk production!)
JWT_SECRET=aquabiodiversa-secret-key-2025

# Server Configuration
PORT=3001

# Untuk production (Render, Railway, dll)
# NODE_ENV=production

